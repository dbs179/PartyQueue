/**
 * PartyQueue UI entry. Bundled by `npm run build:client` into js/dist/main.js
 * (with helpers inlined). Cache-bust via the ?v= query on the script tag in
 * index.html (server injects the app version).
 */
import "./app.js";
